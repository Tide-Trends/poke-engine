[package]
name = "poke-engine-gui"
version = "0.1.0"
edition = "2021"

[build-dependencies]

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }

[build]
